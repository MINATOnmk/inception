
services:
  mariadb:
    image: mariadb_image
    build: ./requirements/mariadb
    container_name : mariadb
    restart: always
    networks:
      - inception_network
    env_file:
      - .env
    volumes:
      - "mariadb_data:/var/lib/mysql"

  nginx:
    image: nginx_image
    build: ./requirements/nginx
    container_name : nginx
    restart: always
    ports:
      - "443:443"
    depends_on:
      - wordpress
    networks:
      - inception_network
    volumes:
      - "wordpress_data:/var/www/html"
    env_file:
      - .env


  wordpress:
    depends_on : 
      - mariadb 
    image: wordpress_image
    build: ./requirements/wordpress
    container_name : wordpress
    restart: always
    networks:
      - inception_network
    env_file:
      - .env
    volumes:
      - "wordpress_data:/var/www/html"

volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/mboulagh/inception_volumes/mariadb_data

  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/mboulagh/inception_volumes/wordpress_data

networks:
  inception_network:
    driver: bridge
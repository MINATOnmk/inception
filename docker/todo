--> mandatory
  -->mariadb
    # bootstrap
    # FLUSH PRIVILEGES; 
    # sql cmd and script cmd
    # mysqld cmd and not mysql
    # EOF way
    # sed -i 's/127.0.0.1/0.0.0.0/g'
    # config default file /etc/mysql/mariadb.conf.d/50-server.cnf

  -->nginx
   *******the port if we add : in last and default port and i use the 444 with https for exempel
   **config file**
    # server bloc and other block http and more
    # ssl
    # ssl handchake
    # location / { try_files $uri $uri/ /index.php?$args;} test case /about  /blog/my-first-post /author/john 
    # fastcgi protocol
    # fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    # location ~ \.php$ 
   **script**
    # -x509 -nodes
    # rsa:2048
    # exec nginx -g "daemon off;"
    # config default file /etc/nginx/conf.d/default.conf 

  -->wordpress
   **docekrfile**
     # what is php8.2  php8.2-fpm php8.2-mysql  mariadb-client curl
     # what is this config file /etc/php/8.2/fpm/pool.d/www.conf
     # wp-cli https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
   **conf file**
     # remove config file and edit the default config file 
     # workers
     # user of wordpress
     # port
     # what is clean env
   **script**
    # while ! mysqladmin ping -h"mariadb" --silent; do echo "MariaDB not ready yet, waiting..." sleep 2 done what is this and is any replace for it
    # all of wp cmd 
    # redis cashe and how it work with wp

  -->docker compose
    # what is docker compose
    # the docker rolated thing keywork rebuild and names
    # why we need it 
    # and how he work 
    # the keywords services volumes networks version
    # the depend block
    # port maping
    # restart
    # how env file work

  -->makefile and docker cmd
    # docker compose -f $(COMPOSE_FILE) up --build -d
    # docker compose -f $(COMPOSE_FILE) down
    # docker compose -f $(COMPOSE_FILE) down --volumes
    # docker compose -f $(COMPOSE_FILE) down --rmi all
    # docker compose -f $(COMPOSE_FILE) logs -f $(c)
    # the other cmd and debug cmd and the famous ones    

  -->network
    # bridge and the other thing
    # namespaces net
    # the other drivers host 
    # the default network

  -->volume



---------------------------

  -->bonus 
  ####### reverse proxy what is it
--> cadvisor
 # what is cadvisor and what is the file needed 
 # and what is bind mount and why we dont use the normal volume (in docker compose for cadvisor)
 # wy this CMD ["cadvisor" , "-logtostderr"]
--> static web site
 # the new edit after the reverse proxy in docekrfile
 
--> adminer
 # the new edit after the reverse proxy in docekrfile
 # who we instll php  and php-mysql 
 # how this adminer work
 
--> redis 
 # the new edit after the reverse proxy in docekrfile
 # what is redis and how he work
 # how he stor data
 # what is protect mode
 # what and the thing we add to the script of wordpress
 # and is this redis work with all webstite or just word press
--> ftp 
 #for later


all of chown have to be study
the cashe of docker and makefile without deep clean